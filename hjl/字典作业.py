# 1.
a = {'p': 80, 'y': 121, 't': 116, 'h': 104, 'o': 111, 'n': 110}
print(a)

# 2.不正确，因为集合也是用花括号括起来的、

# 3.会在这个字典中添加一个新的键值对

# 4.字典更高，因为字典中的键和值组成一个元素，所以在查找时字典每个键值对只用查找一半，而序列要查找整个元素。

# 5.键必须时不可变的，所以键的类型只能是不可变类型，而值可以取任何值、

# 6.{1: ('one', 'two', 'three'), 2: ('one', 'two', 'three'), 3: ('one', 'two', 'three')}
{1: '数字', 3: '数字'}

# 7.是

# 8.dict2=dict1.copy()

# 9.
